# Fichier de configuration local - Copier ce fichier en .env.local et adapter selon vos besoins

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=CHANGE_ME_TO_A_RANDOM_STRING
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# OPTION 1: SQLite (Recommandé pour le développement local)
# Configuration actuelle dans .env - Aucune modification nécessaire
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_dev.db"

# OPTION 2: MariaDB avec Docker (Pour un environnement proche de la production)
# Décommenter la ligne ci-dessous pour utiliser MariaDB via Docker Compose
# DATABASE_URL="mysql://digitalfy:digitalfy_password@127.0.0.1:3306/digitalfy_db?serverVersion=10.11.2-MariaDB&charset=utf8mb4"

# OPTION 3: MariaDB local (si vous avez déjà MariaDB installé)
# DATABASE_URL="mysql://root:password@127.0.0.1:3306/digitalfy_db?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
# Configuration email - Pour le développement, utiliser Mailpit ou Mailtrap
# Mailpit (Docker): smtp://127.0.0.1:1025
# Mailtrap: smtp://USERNAME:PASSWORD@smtp.mailtrap.io:2525
# Gmail: gmail+smtp://USERNAME:PASSWORD@default
MAILER_DSN=null://null
###< symfony/mailer ###

###> Docker MariaDB (optionnel) ###
# Ces variables sont utilisées par docker-compose.yaml
MARIADB_VERSION=10.11
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=digitalfy_db
MYSQL_USER=digitalfy
MYSQL_PASSWORD=digitalfy_password
###< Docker MariaDB ###
